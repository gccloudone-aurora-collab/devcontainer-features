---
name: "Validate Features"

permissions: {}

on:
  pull_request:
    branches:
      - main
    paths:
      - src/**
      - test/**

jobs:
  detect-changes:
    permissions: {}
    runs-on: ubuntu-latest
    outputs:
      features: ${{ steps.filter.outputs.changes }}
      autogenerated_features: ${{ steps.autogenerated_features.outputs.features }}
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          persist-credentials: false

      - uses: dorny/paths-filter@fbd0ab8f3e69293af611ebaee6363fc25e6d187d # v4
        id: filter
        with:
          filters: |
            cert-update: ./**/cert-update/**
            databricks-cli: ./**/databricks-cli/**
            enhanced-docker-in-docker: ./**/enhanced-docker-in-docker/**

      - name: "Select features with autogenerated tests"
        id: autogenerated_features
        run: |
          features="${STEPS_FILTER_OUTPUTS_CHANGES}"
          echo "features=$(echo "$features" | jq -c 'map(select(. != "cert-update"))')" >> "$GITHUB_OUTPUT"
        env:
          STEPS_FILTER_OUTPUTS_CHANGES: ${{ steps.filter.outputs.changes }}

  test-autogenerated:
    permissions: {}
    needs: [detect-changes]
    if: ${{ needs.detect-changes.outputs.autogenerated_features != '[]' }}
    runs-on: ubuntu-latest
    strategy:
      matrix:
        features: ${{ fromJSON(needs.detect-changes.outputs.autogenerated_features) }}
        baseImage:
          - ubuntu:jammy
          - debian:11
          - debian:12
          - mcr.microsoft.com/devcontainers/base:ubuntu
          - mcr.microsoft.com/devcontainers/base:debian
          - mcr.microsoft.com/devcontainers/base:noble
        exclude:
          - features: enhanced-docker-in-docker
            baseImage: mcr.microsoft.com/devcontainers/base:debian
          - features: enhanced-docker-in-docker
            baseImage: mcr.microsoft.com/devcontainers/base:ubuntu
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          persist-credentials: false

      - name: "Install latest devcontainer CLI"
        run: npm install -g @devcontainers/cli

      - name: "Generating tests for '${{ matrix.features }}' against '${{ matrix.baseImage }}'"
        run: devcontainer features test --project-folder . --skip-scenarios -f "${MATRIX_FEATURES}" -i ${{ matrix.baseImage }}
        env:
          MATRIX_FEATURES: ${{ matrix.features }}

  test-scenarios:
    permissions: {}
    needs: [detect-changes]
    if: ${{ needs.detect-changes.outputs.features != '[]' }}
    runs-on: ubuntu-latest
    strategy:
      matrix:
        features: ${{ fromJSON(needs.detect-changes.outputs.features) }}
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          persist-credentials: false

      - name: "Install latest devcontainer CLI"
        run: npm install -g @devcontainers/cli

      - name: "Testing '${{ matrix.features }}' scenarios"
        run: devcontainer features test --project-folder . -f "${MATRIX_FEATURES}" --skip-autogenerated --skip-duplicated
        env:
          MATRIX_FEATURES: ${{ matrix.features }}

  test-global:
    permissions: {}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          persist-credentials: false

      - name: "Install latest devcontainer CLI"
        run: npm install -g @devcontainers/cli

      - name: "Testing global scenarios"
        run: devcontainer features test --project-folder . --global-scenarios-only

  validate:
    permissions: {}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          persist-credentials: false

      - name: "Validate devcontainer-feature.json files"
        uses: devcontainers/action@1082abd5d2bf3a11abccba70eef98df068277772 # v1
        with:
          validate-only: "true"
          base-path-to-features: "./src"
